# DevFlow CLI

DevFlow is a DevOps automation CLI that simplifies Docker and Kubernetes workflows.

## Installation
```bash
pip install devflow
devflow deploy --env dev
devflow deploy --env prod
devflow status
devflow logs
Features
Installable CLI

Docker build automation

Kubernetes deployment

Environment-based configuration


